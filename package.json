{
  "name": "presence",
  "version": "0.0.0",
  "private": true,
  "description": "Presence — AI social marketing platform (monorepo root)",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.5.5"
  },
  "engines": {
    "node": ">=20"
  }
}
